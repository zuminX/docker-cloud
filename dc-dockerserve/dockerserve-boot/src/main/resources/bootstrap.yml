spring:
  profiles:
    active: dev
  servlet:
    multipart:
      enabled: true
      max-file-size: 1024MB
      max-request-size: 1100MB
  application:
    name: dc-dockerserve

common:
  core:
    projectPackage: 'com.zumin.dc.dockerserve'
  web:
    api:
      title: 'Docker服务'
      description: '共享Docker接口'
